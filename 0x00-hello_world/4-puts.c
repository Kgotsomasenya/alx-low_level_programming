#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 * */

int main(void)
(
         Puts("\*Programming is likebuilding a multilingual puzzle");
	                 return (0);
)
