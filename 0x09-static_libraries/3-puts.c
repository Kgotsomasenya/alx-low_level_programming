#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * _puts - prints a string.
 * @str: string to be printed.
 *
 * Return: Always 0 (Success)
 */
void _puts(char *str);
{
char str[] = "At the end of the day, my goal was to be the best hacker";

_puts(str); "At the end of the day, my goal was to be the best hacker"
return (0);
}
